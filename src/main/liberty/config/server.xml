<server description="Sample Liberty server">

  <featureManager>
    <feature>jaxrs-2.1</feature>
    <feature>jsonb-1.0</feature>
    <feature>mpHealth-2.0</feature>
    <feature>mpRestClient-1.3</feature>
    <feature>mpReactiveMessaging-1.0</feature>
  </featureManager>

  <httpEndpoint httpPort="9080" id="defaultHttpEndpoint" host="*" />

  <webApplication location="coffeeshop-service.war" contextRoot="/">

    <variable name="com.ibm.runtimes.events.coffeeshop.http.BaristaService/mp-rest/url" value="${env.APPSODY_HTTP_APPSODY_HTTP_URL}" />

  </webApplication>

</server>
